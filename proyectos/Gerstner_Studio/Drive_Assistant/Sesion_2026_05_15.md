---
tags: [gerstner-studio, drive-assistant, presentacion, sesion]
fecha-creacion: 2026-05-15
estado: ACTIVO — refleja Drive y backend deployados en https://ai.kairosaisolutions.com
relacionado: [[Roadmap]], [[Reorg_Singer_2026_05_14]], [[Funcionamiento]]
commit-ai-gerstner: 9dadeed (paginate list_direct_children)
---

# Sesión 2026-05-15 — Modo Presentación: matching híbrido, UX y mirror

Sesión larga focada en **Modo Presentación**. Tres frentes paralelos:
matching de piezas más permisivo (regex + embeddings), UX (refresh con
SSE, controles persistentes, galerías reactivas), y limpieza estructural
del Drive (mirror Procesos ← Marketing).

---

## 🧠 Matching de piezas — arquitectura final

Pasadas en cascada, salgo en la primera que matchea (`piece_extractor.py`):

1. **Regex estricta** (0ms) — alias literal con word boundaries.
2. **Regex loose** (0ms) — saca stopwords cortas ("de/los/la/y/e") del
   window y del alias.
3. **Regex pegada** (0ms) — concatena sin espacios para tolerar
   transcripciones tipo "marcovidrio" → "marco vidrios".
4. **Embeddings semánticos** (~120ms) — `text-embedding-3-small`,
   precomputado al `/start` en background. Threshold 0.45, margen 0.04
   sobre el segundo lugar. NO alucina porque la salida está restringida
   a `piezas_available`.

### Decisión filosófica clave

En la sesión [[Reorg_Singer_2026_05_14]] habíamos sacado el LLM extractor
por inferir mal en silencios y ruido. Acá *volvimos* a IA pero con
embeddings, no LLM:

> **LLM genera** → puede alucinar slugs, "ayuda" con inputs vagos
> ("ahí va" → apertura_puerta).
> **Embeddings miden** → siempre devuelven el slug más cercano de una
> lista fija + score. Con threshold + margin, inputs vagos quedan
> afuera sin alucinación posible.

### Bug latente fixed

`CONFIDENCE_THRESHOLD = 0.78` venía del LLM viejo que daba 0-1.
Embeddings devuelve coseno 0.45-0.85 → quedaba siempre por debajo y los
matches válidos del embedding se filtraban silenciosamente. Fix:
mapear `cosine 0.45 → 0.80, 0.85 → 0.98` en `piece_extractor.py` antes
de pasarle el score al router.

### Pequeños fixes adicionales

- `EXTRACTOR_MIN_WORDS` bajado de **4 → 2**. "procesos dashboard" (2
  palabras) o "marco vidrios" (2) ahora gatillan el extractor en vez de
  ser filtradas antes. Las layers internas siguen rechazando ruido
  corto, no perdimos precisión.

---

## 🚀 Refresh Drive más rápido con SSE

### Backend

- `drive_service.list_folders_recursive_parallel()` — BFS por nivel con
  thread pool en lugar de recursión serial. **54s → 9.4s** medidos para
  237 carpetas.
- `sync_drive_job(progress_cb)` ahora emite eventos por fase:
  `discovering`, `indexing`, `cache_clearing`, `done`.
- Endpoint `GET /presentation/refresh-drive/stream` (SSE con heartbeat
  cada 15s para sobrevivir proxies).

### Frontend

- Sidebar abre `EventSource`. Barra de progreso 0-100% con gradiente
  azul + texto en vivo: "Descubriendo carpetas… 46", "Indexando 140 /
  237", "Hecho en 9.4s — 0 nuevas, 0 borradas, 0 renombradas."
- Cliente cierra el SSE solo al recibir `done`/`error`. Adiós al
  spinner colgado del POST que timeouteaba en Traefik.

### Bug fixed durante el mirror

`_list_direct_children` no paginaba — usaba `pageSize=100`. Cuando
Procesos Singer pasó de 100 a 115 carpetas (post mirror), 15 quedaron
invisibles en Mongo. Fixed con `pageSize=1000` + loop con
`nextPageToken`. Commit `9dadeed`.

---

## ⚡ Carga rápida de presentación (lazy-load)

Antes: `/start` esperaba al warmup completo de las 190 carpetas
Singer (~7s en cold). Ahora:

- `build_piezas_index()` solo lookups Mongo + canonicalize (~0.1s).
- `warm_piezas_files_background()` corre en `asyncio.create_task`.
- `session.ensure_warmed(piece)` lazy-load on-demand por pieza antes
  de emitir (1-3 carpetas, ~500ms cold).

El usuario ve "Listo, activá el micrófono" casi instantáneo. La primera
pieza nombrada puede tener un delay de ~500ms vs ~100ms hot, pero el
delay se enmascara con el tiempo natural de habla.

---

## 🔍 Buscador global de carpetas (Sidebar)

- Endpoint `GET /presentation/folders` — lista las 237+ carpetas con
  `folder_id`, `name`, `path`. ~30KB JSON, un viaje, ordenado por
  profundidad.
- Endpoint `GET /presentation/folder/{id}/photos` — fotos+videos
  decorados de cualquier carpeta usando el mismo cache que la
  presentación.
- Componente `FolderPicker.jsx` con filtro multi-palabra
  (normalización sin acentos, todos los términos deben matchear).
- Click → abre `PreviewGrid` con las fotos. Click en foto → Lightbox.

---

## 🎨 UX Modo Presentación

### Cambios visuales

- **Botón ✕** fijo arriba a la derecha para terminar presentación
  (antes era browser-back).
- **Mensaje inicial** "Cargando presentación…" (antes "Pre-cargando
  piezas del proyecto").
- **Capitalización con prettifyPiece** (sentence case + nombres
  propios: Hollings, Porsche, Singer; siglas ABS, LED en mayús).
  Aplicado en slide header, navbar pills, timeline, picker.
- **Controles ⏸/🔍 más grandes** (54×48 desktop, 58×52 mobile) con
  borde sutil y fondo translúcido.
- **Overflow "+N"** con la foto siguiente como background semi-opaca
  (opacity 0.42 + veil radial) + "+" y número encima con
  text-shadow. Antes era un gradiente plano.
- **Captions** del filename en thumbs (title attr) y Lightbox (footer
  visible). `lib/filename.js` limpia: saca extensión, reemplaza
  `_`/`-` por espacios.

### Controles persistentes en capa fija (z-index 2000)

Antes los controles vivían dentro de `.presentation` con `position:
absolute z-index: 30`. Al abrir un modal (PreviewGrid 1100, Lightbox
1000), los controles desaparecían. Ahora `.pres-controls-layer` es
`position: fixed z-index: 2000` con `pointer-events: none` (botón
recibe clicks, fondo deja pasar al modal de abajo).

### Reactive gallery/lightbox

Cuando llega `piece_detected`, si hay PreviewGrid o Lightbox abierto:
- PreviewGrid → reemplaza items y título por la pieza nueva
  ("Marco vidrios · Marketing").
- Lightbox → cambia images a la pieza nueva, índice vuelve a 0.

Solo si la pieza nueva tiene fotos (no se queda en blanco). Antes solo
disparaba en voice-detection y filtramos override; ahora dispara
SIEMPRE — el usuario quería que TODO lo visible siga la pieza activa.

### PreviewGrid fixes

- Reusa `ImageCard` (skeleton, fallback al proxy, IntersectionObserver
  para lazy real) en vez de `<img>` plano que rompía en galerías de
  30+ fotos cuando varias thumbnails de Drive fallaban en paralelo.
- `min-height: 0` + `flex: 1 1 auto` en `.preview-grid-body` para
  arreglar el scroll que en flex container sin esa propiedad
  desborda en vez de scrollear.
- Tiles más grandes (`minmax(200px)` antes 160) + `object-fit:
  contain` (no `cover`) para ver fotos verticales completas, no
  recortadas al centro.

---

## 🗂️ Mirror Procesos ← Marketing en Drive

Cambios automatizados desde el backend vía Drive API (service account
con scope `drive`):

### Renames (5 carpetas en Procesos)

Mismo nombre normalizado pero literal distinto — el backend ya las
mergea via `_slug()` pero quedaban feas en Drive:

- `Tapizado de Paneles puerta` → `Tapizado de paneles puerta`
- `Tapas Oil y fuel` → `Tapas oil y fuel`
- `faros y guiños` → `Faros y guiños`
- `Espejos retrovisores y vidrios` → `Espejos Retrovisores y vidrios`
- `Carrocería` (NFD) → `Carrocería` (NFC)

### Creates (19 carpetas vacías nuevas en Procesos)

Que existían en Marketing pero no en Procesos: Body, Consola central
freno de manos, Cueros y diseño interior, Equipamiento de cuero,
Espejos retrovisores exteriores, Frenos, Guanteras de puertas, Motor
Singer, Parabrisas, Paragolpes y faldones, Pedaleras y apoyapies,
Ruedas llantas, Salida doble de escape, Spoiler y componentes,
Tapizado de baul, Volante y accesorios, difusores de aire interiores
tablero, espejo retrovisor interior, guiños.

### Cleanup

- Trasheada `Procesos/guiños` (suelta) — la había creado por error;
  Marketing no la tenía suelta sino integrada en `Faros y guiños`.

### Estado final

- **Procesos: 115 · Marketing: 94**
- **0 carpetas de Marketing faltan en Procesos** (mirror completo).
- **22 carpetas en Procesos sin equivalente en Marketing** (Llantas,
  Guanteras, Guardabarros, Bisagras de baul, Spoiler, Mecánica…) —
  pre-existentes con contenido. Pendiente decidir si mergerlas via
  CANONICAL_PIECES o crear las vacías del lado Marketing.

---

## 📁 Archivos modificados

Repo `5antuca/ai.gerstner` — commits `141f768` y `9dadeed`:

```
backend/app/routers/presentation.py         # SSE refresh + endpoints /folders /folder/{id}/photos
backend/app/services/drive_service.py       # BFS paralelo + paginate >100
backend/app/services/sync_service.py        # progress_cb
backend/app/services/presentation_warmup.py # build_piezas_index + warm_piece + warm_piezas_files_background
backend/app/services/presentation_session.py # folder_ids_by_slug + ensure_warmed
backend/app/services/piece_embeddings.py    # NUEVO — matcher semántico
backend/app/services/piece_extractor.py     # encadenar embeddings + score mapping
backend/app/services/presentation_pieces.py # 3 pasadas regex + aliases ampliados
frontend/src/components/Sidebar.jsx         # SSE progress + buscador
frontend/src/components/FolderPicker.jsx    # NUEVO
frontend/src/components/PreviewGrid.jsx     # ImageCard + scroll fix
frontend/src/components/PresentationSlide.jsx # +N con foto fondo + prettify
frontend/src/components/PresentationTimeline.jsx # prettify
frontend/src/components/PiecePicker.jsx     # prettify
frontend/src/components/ImageCard.jsx       # title con filename
frontend/src/components/Lightbox.jsx        # caption visible
frontend/src/lib/pieces.js                  # NUEVO — prettifyPiece
frontend/src/lib/filename.js                # NUEVO — cleanFilename
frontend/src/pages/Presentation.jsx         # ✕ exit + reactive gallery/lightbox
frontend/src/App.jsx                        # FolderPick handler
frontend/src/styles.css                     # capa controles + overflow nuevo + preview-grid contain
```

---

## 🚧 Carry-over para próxima sesión

Ver detalle en [[Roadmap#Próxima sesión — carry-over 2026-05-15]].

Resumen ultra-corto:
- Telemetría real del matching (collection `piece_match_events`).
- Botón de feedback "❌ no era esa" sobre la slide.
- Decidir mirror reverso Marketing ← Procesos (22 vacías opcionales).
- Aliases: agregar "asientos" para butacas (vocabulario rioplatense).
- Golden set + regresión cuando haya ~50 eventos labeled.
